## google sheet config

# key for the google sheet (can be obtained from URL)
google_sheet_key = ''

# worksheet name; `Transactions` is the Tiller default
worksheet_name = 'Transactions'

# range of transaction data in the above sheet; `B1:0` is the Tiller default
worksheet_range = 'B1:O'

## script config

# filename that the script will use to write a CSV of the google sheet data
csv_filename = 'tiller_transaction_data.csv'